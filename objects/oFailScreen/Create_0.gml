isActive = false;

//nextLevelButton = instance_create_layer(x+(sprite_width / 2) - (oNextLevel.sprite_width / 2),y + 400,"WinFailScreenButton",oNextLevel);

retryButton = instance_find(oRetryFail, 0);
menuButton = instance_find(oToMenuFail, 0);

direction = 90;